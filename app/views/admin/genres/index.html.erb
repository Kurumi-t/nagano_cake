<h1>ジャンル一覧・追加</h1>
<div class="field">
  <%= form_with model: List.new do |f| %>
    <h4>ジャンル名</h4>
    <%= f.text_field :name %>
    <%= f.submit '新規登録' %>
	<% end %>
</div>
<h4>ジャンル名</h4>
<% @genres.each do |genre| %>

<% end %>

